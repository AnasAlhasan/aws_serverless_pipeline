# aws_serverless_pipeline
This repo contains a policy file and python file. python file is meant to be the lambda function that calls the aws service "Rekognition" that analyze an image when its uploaded to a s3 bucket, and generate a json file containing the ouput on s3 bucket. the policy provides minimal access to resources for the function.
